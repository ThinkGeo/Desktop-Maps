<UserControl x:Class="ThinkGeo.UI.Wpf.HowDoI.Samples.RasterMapProviders"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:thinkGeoUi="clr-namespace:ThinkGeo.UI.Wpf;assembly=ThinkGeo.UI.Wpf"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="260"/>
        </Grid.ColumnDefinitions>

        <thinkGeoUi:MapView x:Name="Map" Grid.Column="0" Loaded="Map_Loaded"/>
        <!-- The keyed providers are a page over the map: without your key none of them draws. -->
        <ContentControl x:Name="KeyedProvidersPage" Grid.Column="0" Visibility="Collapsed"/>

        <StackPanel Grid.Column="1" Background="DimGray" Margin="10">
            <TextBlock Text="Provider" FontSize="16" FontWeight="Bold" Foreground="WhiteSmoke" Margin="0,0,0,6"/>

            <RadioButton x:Name="ThinkGeoLight" Content="ThinkGeo Cloud - Light" IsChecked="True" Checked="Provider_Checked"/>
            <RadioButton x:Name="ThinkGeoDark" Content="ThinkGeo Cloud - Dark" Checked="Provider_Checked"/>
            <RadioButton x:Name="ThinkGeoAerial" Content="ThinkGeo Cloud - Aerial" Checked="Provider_Checked"/>
            <RadioButton x:Name="ThinkGeoHybrid" Content="ThinkGeo Cloud - Hybrid" Checked="Provider_Checked"/>
            <RadioButton x:Name="Osm" Content="OpenStreetMap" Checked="Provider_Checked"/>
            <RadioButton x:Name="KeyedProviders" Content="Azure Maps, Google Maps, HERE and Mapbox" Checked="Provider_Checked"/>

            <TextBlock x:Name="Status" Foreground="#DDDDDD" FontFamily="Consolas" FontSize="11"
                       Margin="0,12,0,0" TextWrapping="Wrap"/>

            <TextBlock Foreground="#DDDDDD" TextWrapping="Wrap" FontSize="11" Margin="0,12,0,0"
                       Text="ThinkGeo Cloud and OpenStreetMap are one tile source each, handed to the style. The commercial providers need your own API key, so they are a page of their classes and one-line usage rather than a map."/>
        </StackPanel>
    </Grid>
</UserControl>
