<UserControl x:Class="ThinkGeo.UI.Wpf.HowDoI.OverviewMap" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:thinkGeoUi="clr-namespace:ThinkGeo.UI.Wpf;assembly=ThinkGeo.UI.Wpf" 
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800" Loaded="OverviewMap_OnLoaded">

	<UserControl.Resources>
		<Style x:Key="ImageButtonNoHoverStyle" TargetType="Button">
			<Setter Property="OverridesDefaultStyle" Value="True" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Button">
						<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
	</UserControl.Resources>

	<Grid>
		<thinkGeoUi:MapView x:Name="MainMap"/>

		<Button x:Name="CompassButton" 
                Style="{StaticResource ImageButtonNoHoverStyle}" 
                Width="40" Height="40" Background="Transparent" 
                HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10" 
                Click="CompassButton_Click" ToolTip="Reset North" RenderTransformOrigin="0.5,0.5">
			<Button.RenderTransform>
				<RotateTransform Angle="{Binding ElementName=MainMap, Path=RotationAngle}" />
			</Button.RenderTransform>
			<Image Source="/Resources/icon_north_arrow.png" Stretch="Fill" />
		</Button>

		<Border BorderBrush="#FF333333" BorderThickness="5" CornerRadius="6" Margin="8" Width="220" Height="220" 
			        HorizontalAlignment="Left" VerticalAlignment="Bottom" >
			<thinkGeoUi:MapView x:Name="PreviewMap" />
		</Border>
	</Grid>
</UserControl>