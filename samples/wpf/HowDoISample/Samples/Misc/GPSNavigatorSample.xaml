<UserControl x:Class="ThinkGeo.UI.Wpf.HowDoI.Misc.GPSNavigatorSample"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ThinkGeoUI="clr-namespace:ThinkGeo.UI.Wpf;assembly=ThinkGeo.UI.Wpf"
        mc:Ignorable="d"
        Loaded="UserControl_Loaded">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center">North Up</TextBlock>
        <TextBlock Grid.Row="0" Grid.Column="1" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center">Car Head Up</TextBlock>
        <ThinkGeoUI:MapView x:Name="mapView1" Grid.Row="1" Grid.Column="0" BorderThickness="3" BorderBrush="White"></ThinkGeoUI:MapView>
        <ThinkGeoUI:MapView x:Name="mapView2" Grid.Row="1" Grid.Column="1" BorderThickness="3" BorderBrush="White"></ThinkGeoUI:MapView>
    </Grid>
</UserControl>
