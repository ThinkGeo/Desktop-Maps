<UserControl x:Class="ThinkGeo.UI.Wpf.HowDoI.HatchStyles"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:thinkGeoUi="clr-namespace:ThinkGeo.UI.Wpf;assembly=ThinkGeo.UI.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="210" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <thinkGeoUi:MapView Grid.Column="0" Grid.Row="0" x:Name="MapView" Loaded="MapView_Loaded" />

            <Border Grid.Column="0" Width="330" Background="Gray" VerticalAlignment="Bottom"
                     HorizontalAlignment="Center" Margin="0,0,0,20" CornerRadius="0" Padding="5">
                <CheckBox x:Name="ChkLiveRender" Content="Rendering on WpfDrawingOverlay"
                     VerticalContentAlignment="Center" FontSize="20" Foreground="White"
                     Background="White" Checked="ToggleButton_OnChecked" Unchecked="ToggleButton_OnChecked"/>
            </Border>

        </Grid>

        <Border Grid.Column="1" Background="Gray" Padding="5" HorizontalAlignment="Right"
            VerticalAlignment="Stretch" BorderBrush="DarkGray" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Select a Hatch Style:" FontSize="16" Foreground="White" 
                           TextWrapping="Wrap" Grid.ColumnSpan="2" Margin="0,10,0,0"/>

                <ComboBox Grid.Row="1" x:Name="CboHatchStyles" VerticalAlignment="Top" HorizontalAlignment="Center"
                          Background="Gray" Width="200" Height="30" FontSize="18" SelectedIndex="0"
                          SelectionChanged="CboHatchStyles_OnSelectionChanged" Margin="0,10,0,0">
                    <ComboBox.ItemContainerStyle>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="Width" Value="190"/>
                            <Setter Property="IsHitTestVisible" Value="True"/>
                            <EventSetter Event="PreviewMouseMove" Handler="ComboBoxItem_PreviewMouseMove"/>
                        </Style>
                    </ComboBox.ItemContainerStyle>
                </ComboBox>
            </Grid>
        </Border>
    </Grid>

    <UserControl.Resources>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="WhiteSmoke"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="WhiteSmoke"/>
        </Style>

        <Style TargetType="Grid">
            <Setter Property="Background" Value="DimGray"/>
        </Style>
    </UserControl.Resources>
</UserControl>